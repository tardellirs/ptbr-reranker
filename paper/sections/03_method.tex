\section{Method}
\label{sec:method}

% Cover:
% - Cross-encoder architecture (concatenated query + passage → single score).
% - Base model choice (Albertina-100m PT-BR).
% - Hard negative mining procedure (Serafim-IR bi-encoder, rank 10-100, 7 negs).
% - Loss function (softmax cross-entropy over {positive, hard negatives}).
% - Training hyperparameters.

\textbf{TBD.}
